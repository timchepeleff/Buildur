<!DOCTYPE html>
<html>
  <head>
    <title>Tim's Breakable Toy</title>
    <link href='http://fonts.googleapis.com/css?family=Open+Sans|Raleway' rel='stylesheet' type='text/css'>
    <%= javascript_include_tag "path/to/highcharts.js", "chartkick" %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <% if current_user %>

      <div class="row site-container">
        <div class="12-large columns">
          <nav class="top-bar" data-topbar>
            <ul class="title-area">
              <li class="name">
                <%= link_to image_tag('buildur-logo.png', size:"300"), root_path %>
              </li>
            </ul>
            <section class="top-bar-section">
              <!-- Right Nav Section -->
              <% if current_user %>
                <ul class="right">
                  <li>
                  <%= form_tag(users_path, method:"get") do %>
                    <%= text_field_tag(:query, nil, placeholder: "Search...")%>
                    <%= submit_tag("Search", :style => "display: none;", :id => "hiddensubmit") %></li>
                  <% end %>
                </ul>
              <%end%>
            </section>
          </nav>

          <%= yield %>
        </div>
      </div>

      <footer>
        <div id="footer">
          <% if current_user %>
                <div class="6-small small-offset-2 columns">
                  <ul>
                  <li id="avatar-footer-left"><%= link_to image_tag(current_user.avatar_url, size:"50"), user_path(current_user) %></li>
                  <li id="avatar-footer-left-edit"><%= link_to image_tag("edit_user.png", size:"37"), edit_user_path(current_user) %></li>
                  <li id="avatar-footer-right"><%= link_to image_tag("sign_out.png", size:"37"), destroy_user_session_path, method: :delete %></li>
                </ul>
                </div>


              <%end%>
        </div>
      </footer>
    <%else %>
      <div class="login-wrapper">
        <div class='container'>
          <div class='logo_holder'>
            <%= yield %>
          </div>
        </div>
      </div>
    <%end%>
  </body>
</html>


